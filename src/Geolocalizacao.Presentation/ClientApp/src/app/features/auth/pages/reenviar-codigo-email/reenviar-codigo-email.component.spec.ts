import { TestBed, ComponentFixture, ComponentFixtureAutoDetect, waitForAsync } from '@angular/core/testing';
import { BrowserModule, By } from "@angular/platform-browser";
import { ReenviarCodigoEmailComponent } from './reenviar-codigo-email.component';

let component: ReenviarCodigoEmailComponent;
let fixture: ComponentFixture<ReenviarCodigoEmailComponent>;

describe('reenviar-codigo-email component', () => {
    //beforeEach(waitForAsync(() => {
    //    TestBed.configureTestingModule({
    //        declarations: [ ReenviarCodigoEmailComponent ],
    //        imports: [ BrowserModule ],
    //        providers: [
    //            { provide: ComponentFixtureAutoDetect, useValue: true }
    //        ]
    //    });
    //    fixture = TestBed.createComponent(ReenviarCodigoEmailComponent);
    //    component = fixture.componentInstance;
    //}));

    //it('should do something', waitForAsync(() => {
    //    expect(true).toEqual(true);
    //}));
});
